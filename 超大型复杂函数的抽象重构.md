# 业务场景
使用excel进行用户数据的批量批量导入，常规字段约为10个，用户的其他拓展属性约为15个。用户通过填写excel后，导入系统进入批量的导入用户

# 当前表现
函数逻辑繁碎，单个函数超过2000 行，主要逻辑是在执行每个字段的存在性判断，转换，写入，代码复用率低下。性能也非常低下，三十万个用户的导入可能需要耗费7-8小时，性能非常低下，，原逻辑设计原本只针对常规字段的导入，原本数量较少，进行后续字段的扩展后，每一个字段都在原本的函数中加入逻辑，导致调用链越来越长。

# 重构思路
抽象字段逻辑，采用接口行为管理，由每个字段实现自己的逻辑。

# 结构属性

```go
type Filed interface {
	PreProcess(raw string) //数据预处理，去空格，导入存在性检查
	Action()
	LogRecord() //用于输出格式化的输出结果 
}
```

```go
type Fileds struct {
	FiledName string
	FiledKey string
}


// Fileds implement Filed interface
```

全局字段管理器与映射管理器
```go
UserMap:=map[string]string {
	"": ""
}
```