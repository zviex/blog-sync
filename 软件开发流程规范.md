## Model 层次隔离

初步而言对于数据库的Model 结构体，是一定要提供最小引用的包来提供层次隔离，什么是最小引用？就是Model的定义中所有的类型必须是单向类型，比如int这种基础类型，自己封装的引用了基础类型的二次单向类型。因为这样可以保证model层的单向性，不会引发循环依赖问题

## 实现接口需要进行编译期检查

```go
type A interface {
	Do()
}

type B struct {}

func (b B) Do () {}

var _ A = (B)(nil)
```

常见就是利用类型断言，断言类型是否匹配，避免将panic引入到运行期

## Clean Code

对于go这种类C语言，本身提供了面向过程，面向对象的两种编程范式，目前更推荐面向对象，提供良好的封装可以使代码扩展性和可阅读性提高。

面向对象和面向过程在代码可读性方面有一个非常大的共同点，就是基本逻辑的封装，对于常规模块级别逻辑，需要一个极其精简的代码能充分概括模块的功能，通过层级式封装，充分隐藏实现，而暴露逻辑。

最常见的规范就是，函数或方法的粒度，一定要确保一个函数干一件事，顶层逻辑依赖若干个子逻辑，避免单个函数的逻辑冗长，比如常规的读取一个文件内的文字是否等于某个值，一个合格的封装是封装了读取文件的逻辑，由一个函数直接返回一个结果，进行比较，如果把读取逻辑嵌入到顶层逻辑，整体逻辑非常难看。


封装的依赖倒置原则，对于如面向对象的接口，如果需要封装，函数的参数一定不能依赖与具体的实现。

举一个简单的例子，如果一个数据库服务层，依赖了具体的数据库结构，比如bun.Tx，那么这个封装是无效的，因为没有屏蔽底层细节，如果那天我需要更换一个orm框架，那么这个接口，依赖与这个接口的方法，和接口实现，全部都要更改。

模块只暴露接口，不暴露实例



## 使用SDK方式彻底解决消息队列引用黑洞
多模块消息队列的元信息集中管理，不推荐采用各个服务自己进行原生客户端进行消息推送，消息接受。必须在中间库做到`Topic定义`，`推送包装`，`接受包装`。这样能够彻底避免各个服务之间的Topic Key的重定义，以及彻底根除消息推送的消息路径黑洞问题，可以快速寻找到一个Topic的消费者有哪些，防止进入消息推送黑洞（如在代码仓库权限隔离情况下，无法进行跨仓库搜索时候，完全无法得知有那些模块接入了这个消息）

## 使用Map代替Switch
我们在使用策略模式时候，对于注册策略或其他情况，使用Map代替switch进行处理，会得到最小的侵入性，当我们直接采用封装的注册方法注册后，立即可以使用。但是使用switch的话，每添加一个新的策略就要在switch里面新增一个case

## 提前返回

为了避免if地狱，一层层的套if，对不合法的直接提前返回。
```go
if len(data)== 0 {
	slog.Error("error)
	return
}

// do
```

# 单一职责与开闭

对于粒度越细的组件，如功能函数设计，这种保持单一职责与开闭原则较为容易。但是在顶层架构设计是，单一职责和开闭设计变得极为重要。这个更考验人的架构能力。在顶层架构设计中，保持单一职责和开闭设计的过程如下。程序的结构流转不依赖具体的模块，每个模块保持其功能，如果出现了顶层模块出现了比如if x== b来处理下层结构时，这个时候证明该逻辑需要下沉到具体的子逻辑里面
