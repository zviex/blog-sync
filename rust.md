# 基本数据类型
rust基本数据类型与常规语言关键字不同，他没有int一说，只有i+长度的语法，比如i8，i16，无符号数就是使用u代替i，u8,u16 浮点数也是f开头，有f64,f32,然后就是bool， char

特例：
在rust中，string属于std::string，不属于基本类型，且存在两种&str和堆分配的String，一些短的，比如一些key，这些是可以存在于栈上，对于动态的，比如string拼接，这些都是要在堆上操作。


所有权类型：
这个是rust的核心，也是rust最别扭，最难以理解的部分，其实本质上就是常规的非gc语言，如c和cpp，都是采用手动分配内存，然后回收的，这个处理的是堆的问题，即使在有gc的情况下，栈上的都是离开作用域就进行销毁了。rust引出所有权，本质就是把堆上的数据也和栈一样，当你离开作用域后，也进行销毁。rust的所有权本身涉及到两个，copy和move，然后就是&引用所有权，本质就是获取一下，不拿到所有权。copy和move所有权发生在参数传递或者显式调用，在参数传递时候，除了基本类型，所有的类型基本上都是move，即一旦传递进入另一个函数，所有权即被这个函数接管，当这个函数执行完毕后，你想再访问这个数据，就无法访问到了，这个时候处理办法就是把这个数据又返回来，或者采用可变引用，在不改变所有权的情况下，进行处理。

闭包
rust的闭包不同于golang的闭包，在rust中，闭包不等价于函数，但是在功能上等价。比如以下两种就是等价
```rust
fn do() -> i8 {
	8
}

let closure = || 8;
```

带参数的闭包
```rust
fn do(arg:i8) -> i8 {
	arg*1
}

let closure = |arg| arg*1;

```

# 宏（Marco）
宏本质上是一种批处理操作，用于生成动态的模板代码，以达到运行期的0开销，比如非常经典的Rust的模板代码宏：`Serialize`，其实golang中也可以做到类似于宏的，比如go generate命令。
`Serialize`宏本身来说与具体的序列化行为是完全解耦合的。`Serialize`本身做的功能，就是生成了一段实现trait的代码，然后具体的序列化库，会直接读取硬编码的结构体属性数据，拿到结构体本身的属性，并获取值，然后选择具体的序列化器进行序列化。

在rust运行过程中，运行时是无法做到反射行为的，在Go和Java里面，我们可以通过反射，在运行期获取到一个结构体的属性。比如一个结构体中，我含有三个属性，我可以通过反射获取到有几个属性，那些方法，属性名称是什么等等属性。但是Rust不允许，Rust做的就是通过宏展开，他通过编译期间阅读了AST，直接把一个结构体属性直接硬编码到代码中，这样可以直接通过方法在无任何运行时开销的情况下，获取到属性，做到了完全的0开销反射。

在go和Java中，一个结构体本身在比如进行序列化这种行为时，就是要完全获取一个结构体的所有属性，所以不可避免的引发反射，这样也造成了性能降低等原因。