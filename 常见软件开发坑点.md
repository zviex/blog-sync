1. gorm
   gorm本身会忽略默认值的更新，比如bool的false，int的0.当想进行修改为默认值，比如配置的关闭，内容的置空，会出现无效的情况。常见的使用办法是采用map强制更新。
   对于部分有强制性置默认值的需求的，因为gorm的gorm.db的链式调用，可以本地封装一个强制更新的方法，内容就是需要强制更新的字段，这样我们可以选择配置那些字段需要有强制更新的能力
2. Pg的自定义类型的转换。
   这个的本质也是结构化数据的序列化与反序列化。当封装的结构体数据想通过model直接使用，就必须指定这个类型在数据库的序列化与反序列化方法，也就是在数据库中如何存储这个结构，如何读取这个结构。
   要实现的接口有如下
3. 全局错误定义和返回，对于错误返回，尤其是调用基础层应用如net包下返回的错误，默认采用log记录返回错误，不可返回原始错误，返回内部定义错误如`服务器内部错误`，避免底层错误暴露细节或给用户带来困扰，出现即打印，打印后不在使用
4. 修改时包含全量结构和增量结构时，业务逻辑优化方案
   后端diff+hook。至于为什么前端传递修改字段，这个在后端看来是过时的，在有多个用户的情况下，可能存在以下比较属性过期的问题，比如由于存在系统定时任务等修改了属性，但是前端进行diff时，拿取的是可能1分钟前的记录和当前提交的记录进行比较，这个时候的diff是不准确的，只有后端select 且加排它锁后，获取的数据进行diff，这样得到的才是事实的变更属性。这样hook才不会失效

# 跨域资源漏洞

配置跨域白名单或禁止跨域