# 杂项
## 依赖仓库

当我们遇到一些比如需要进行服务拆分，或多模块的情况，会出现依赖我们自己开发的服务的情况。这个时候对于gomod内的项目命名就有一定规范。`go.mod`内部的项目命名是一定要可以通过该地址访问的。

尤其是在一些新建了组织的情况下，一定要注意这个仓库的地址。

如果git的域名为`internal.git.com`你的仓库直接在git服务中新建仓库为test。那么你的gomod中对于该项目的命名就要为`internal.git.com/test`。当你新建了组织，你在组织下的仓库test，组织为A。那么你的项目命令就要为`internal.git.com/A/test`。因为golang的依赖服务本质是依赖于git的。所以你的依赖包路径必定需要严格可访问（这里的可访问是指在权限足够的情况下的正确地址）然后你需要配置go的noproxy地址和goprivate地址。防止你配置的代理尝试去拉取无法拉取到，这种在内网部署的更是重要。

# 技巧

## 切片复制/追加

对于golang的切片，如果从另一个切片追加数据到新切片，且两者类型相同，可以直接采用append两个切片的方式，这样效率比直接单个追加的效率要高得多。


# OLAP

数据库在现代的环境中，常见的有mysql这种关系数据库，nosql的Redis这种内存数据库，还有用户数据分析的数据库如时序数据库和olap的作为商业BI分析引擎的数据库如clickhouse这种


k8s ingress代理网关，可以作为rpc网关桥接客户端与服务，同时可引入处理器进行比如权限，限流等


## Option模式
option模式的选择性构造或处理链，此种操作在尤其是在一些数据流上作用最大，比如web请求结构体，可能需要大量扩展的函数，比如今天是一个常规的string赋值，第二天需要增加空格过滤，第三天需要进行特殊字符过滤。

```go

type Option func(*Server)

func WithAddr(addr string) Option {

    return func(s *Server) {
        s.Addr = addr  
    }
}

func NewServer(opts ...Option) *Server {
    srv := &Server{Addr: "127.0.0.1", Port: 8000}
    for _, opt := range opts {
        opt(srv)
    }
    
    return srv
}

```

## 原子指针的客户端并发保护与单例模式
#### 饿汉式
以任何方式在初始化时，就已经初始化好了，可以通过封装方法直接访问的单例。天然线程安全

#### 懒汉式
支持延迟加载的初始化，即当第一次使用时，才进行加载，线程不安全，当进行多线程调用获取时，可能存在初始化多个客户端。此时golang提供了原语sync.once。这个可以保障一个方法只会执行一次，比如对初始化函数进行包装，则保证变量只会被初始化一次。
```go
var once sync.Once 
var db *DB 
func GetDB() *DB { 
	once.Do(func() { 
		db = connectToDatabase() // 只会执行一次 
	}) 
 return db 
}
```
### 配置热更新与原子指针

在slog库中，存在以下的操作
```go
var defaultLogger atomic.Pointer[Logger]

func Default() *Logger { 
	return defaultLogger.Load() 
}

func SetDefault(l *Logger) {
	defaultLogger.Store(l)
	if _, ok := l.Handler().(*defaultHandler); !ok {
		capturePC := log.Flags()&(log.Lshortfile|log.Llongfile) != 0
		log.SetOutput(&handlerWriter{l.Handler(), &logLoggerLevel, capturePC})
		log.SetFlags(0)
	}
}
```

本质这个就是饿汉式实例+可线程安全变更的实现，由于本身提供了在初始化完成default 实例后可变更实例的接口，同时有可能是多线程情况下竞争，所以引入了atomic。



```go
//使用
fmt.Fprintf(io.writer,format,any)
//替代
x.write(fmt.Sprintf("format string",any))
```


批量操作采用case when，不推荐采用for 循环update或insert



# 代码健壮性

## 结构体的封装
对于一些结构体属性，提供Get支持访问，同时每个方法进行nil结构的判断，避免直接进行引用结构体访问，可以避免很多的空指针panic，避免安全问题。

```go
type Config struct {
  A string
}

func Do() {
  var c *Config
  // 不安全示例
  a:=c.A
  
  // 安全示例
  a=c.GetA()
  
  // 繁琐示例，不进行包装，遍地nil判断
  if c == nil {
    a=""
  }
  a=c.A
}


func (c *Config) GetA() string{
  if c==nil {
    return ""
  }
  return c.A
}

```



## 泛型与any/interface{}
使用泛型对一些不安全的断言行为进行包装，避免断言散落各处与断言的if ok检查冗余

```go
func GetMapValue[T any](m map[string]interface{}, key string) (T, bool) {
    var zero T
    if v, ok := m[key]; ok {
        if typed, ok := v.(T); ok {
            return typed, true
        }
    }
    return zero, false
}

// 使用示例
data := map[string]interface{}{"age": 25}
if age, ok := GetMapValue[int](data, "(age"); ok {
    fmt.Println("Age:", age)
}
```


对于Go提供的any（interface{}）的结构，是在偏向底层需要进行类型降级时才需要做到的。一个合格的api，是绝对不允许直接暴露any到上层的。而泛型就是解决这个问题的方法。对于编程语言中的结构来说，没有那个结构是没有类型信息的，即使是any也有类型信息，但是被降级了。所以如果出现具体类型的断言，就需要此处函数设计是否合理？最好的方法就是类似上面的，屏蔽断言行为，使用泛型这种具体类型信息去包装，让api更为安全。



## 安全性封装访问

一个结构体的字段访问，一个安全的Get封装如下

```go
func (x *TestStruct) GetId() *v1.Id {
	if x != nil {
		return x.Id
	}
	return nil
}
```

这种封装在一些设计到层层指针结构套用的情况下，非常的安全有效，无需担心nil refer panic，而且无需频繁的对一些指针判nil，大大减少代码量，这里尝试使用protobuf进行生成，尤其是对于比如数据库的model文件，因为常规业务里面，对于数据库的model操作是最多的。





## 使用sdk方式彻底解决消息队列引用黑洞

### 黑洞表现

各个业务都直接采用官方封装的客户端，自己定义一些消息主题等元数据，然后对接方就是直接复制消息主题，保持一致。从开发角度来讲，是正确的，能完成任务。但是对于软件架构来说，这个代码对于非所属开发人员来说，就是一个完全性黑盒，但从一个项目，完全无法得知有那些消费者对接了这个消息主题，如果严重一点的话，可能某个主题推送后，可能涉及一些财务，或者删除动作，但是开发者只能通过一些工具去查找接入来那些消费者，如果这个时候消费者的名称或标识符命名很差的话，引入来极大的开发和后续维护难度。



### 解决方案

多模块消息队列的元信息集中管理，必须在中间库做到`Topic定义`，`推送包装`，`接受包装`。这样能够彻底避免各个服务之间的Topic Key的重定义，以及彻底根除消息推送的消息路径黑洞问题，同时可以利用lsp快速寻找到一个Topic的消费者有哪些，防止进入消息推送黑洞。

从代码封装角度来讲，一个业务逻辑时不需要知道自己的消息主题是什么的，最简单的方法就是一个方法推送或获取，其他的内容都进行封装，大大提升来可维护度。

## 使用Map代替Switch

这个是很多语言中，我们常常用到的一个技巧。
我们在使用策略模式时候，对于注册策略或其他情况，使用Map代替switch进行处理，会得到最小的侵入性，当我们直接采用封装的注册方法注册后，立即可以使用。但是使用switch的话，每添加一个新的策略就要在switch里面新增一个case



gorm的preload关联与joins关联


# 任何语言中指针的双面性
我们知道指针可以直接指向对象，通常我们习惯用指针来避免一些拷贝。但是大量的，频繁的小对象，对任何语言系统性能都是致命打击，比如无gc的c，rust；有gc的go，java等。因为他们都涉及到一个关键点，就是非顺序性的小对象内存释放。每个小对象的地址都不连续，导致cpu频繁cache miss，去定位这些小对象。这种尤其发生在比如指针数组。

现在有如下场景，读取数据库的1w条数据到内存，进行后续处理，采用指针数组的形式存储数据

`无gc`：指针数组的释放，需要进行遍历整个列表，对每个内容进行free，一共free一万次，同时带来大量内存碎片。
`有gc`：指针数组释放，涉及到gc的性能敏感点，gc需要进行频繁的释放，gc无法一次性释放完全1w个对象，导致gc抖动，软件卡顿。

在遇到大量，频繁的对象分配时，尤其是数组型，严格确定不能采用指针数组，这会带来大量的小对象，尤其是在扫描数据库数据时。

当然以上前提均在堆上，这要引出另外的一个问题，为什么栈比堆快，栈和堆为什么会和数据结构里面的栈和堆是一样的名字。

### 栈
在数据结构中，栈是先进后出，操作成为压栈和弹栈。栈区也是这样，他的每个元素本质就是函数调用，调用则压栈，结束则弹栈，为什么上面说的大规模的指针滥用会造成gc抖动和性能降低，也是为什么大量节点的链表对计算机而言并不友好这个问题为什么仅限于堆内存？
首先明白什么是指针的滥用，指针的滥用主要是指的堆内存上大量的离散的，小的对象引用，因为这种会造成堆内存的大量内存碎片，以及带来大量的cache miss，cpu缓存失效。
那你说，那我不用指针，或者尽量少用指针，能不用就不用，这样也不对，指针本来就是给我们的一个提高性能的利器，为什么不用呢？指针最好的实践，就是用在栈上，还是回到上面的问题，一万个数据库对象的指针数组，如果它在栈上，那么并不会造成很大的性能影响，因为当栈帧弹出时，是一片区域被free了，栈不用管你的数据在哪里，整片的给你释放掉，并不会造成很大的影响。因为栈帧就是代表了这个函数内部的资源的周期，当前栈帧弹出就代表这个函数栈区域可以进行弹出，并不关心你的一万个内存指针在这个函数栈区域的何处。

这个答案也是为什么栈要比堆快的答案，因为栈是以栈帧为单位的批量释放，而堆由于共享特性，无法进行一个区域内存在个各个生命周期不同的对象，无法做到批量释放，同时栈的malloc还需要进行内存碎片区域的查找，通过算法找到一个可以存入当前结构的地址，当大量的小对象进行堆分配时，除了分配，算法空间寻找等开销外，还会带来释放后的碎片特性，导致内存利用率降低。