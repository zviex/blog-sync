---
title: 计算机网络
tags:
category:
created: 2026-09-20
updated: 2026-09-20
---

# ACL策略

访问控制列表（Access Control List，ACL），是一种基于包过滤的访问控制技术，它可以根据设定的条件对接口上的数据包进行过滤，允许其通过或丢弃。访问控制列表被广泛地应用于路由器和三层交换机，借助于访问控制列表，可以有效地控制用户对网络的访问，从而最大程度地保障网络安全。

![](https://biavdbz3ln.feishu.cn/space/api/box/stream/download/asynccode/?code=YjAwZTRlZjQ0MGZmZThhODQ1M2RhMDliYjgyZTBkOWFfeUlaYWxEWXlDRUVWMzBma0lWWjRrQjF3aTgydUVGd1NfVG9rZW46R3FpOGJha0x6b1NBWjJ4T0RlZmNYNGNabnFmXzE3MzU1MzczODY6MTczNTU0MDk4Nl9WNA)

  

## ACL应用：

1. NAT
   
      NAT时配置acl策略可以防止某些外部地址通过NAT访问内部资源，可以精细控制那些地址可以访问资源

2. 防火墙
   
      这里要区分一下防火墙策略和acl策略，acl策略主要运行层次在第3层传输层，而防火墙可以从数据链路层到最顶层应用层的全管理，因此防火墙比较消耗性能，而acl更加轻量，在防火墙上使用acl充分利用acl的轻量级特性和防火墙的全管理，更加精细化配置策略

3. Qos流策略
   
      Qos流策略控制可以更加精细化配置流量的优先级，比如带宽有限时配置支付，游戏等流量更高的优先级，对其他如下载，上传等耗时大规模占用带宽操作进行限速。


## IP地址分类计算和子网掩码及CIDR记法

五种地址分类及其二进制地址标识号

`A类`：0 （1.0.0.0-127.255.255.255）0 0000000-0 1111111=1-127

`B类`：10 （128.0.0.0-191.255.255.255）10 000000- 10 111111= 128 -191

`C类`：110 （192.0.0.0-223.0.0.0）110 00000- 110 11111=192-223

`D类`：1110 （224.0.0.0-239.255.255.255）1110 0000 - 1110 1111=224-239

`E类`：11110 （240.0.0.0-247.255.255.255）11110 000- 11110 111=240-247

  

子网掩码及其CIDR记法

如191.168.1.0/10，后面的/10为CIDR记法，表明子网掩码的前10位为全1，则该IP的子网掩码为11111111 11000000 00000000 00000000，掩码为225 192 0 0。

知道了掩码，则主机数量有2^22-2个，减去广播地址和网络地址。

网络号=IP地址AND掩码

主机号=IP-网络号

## DHCP与ARP的mac认证过程

### DHCP 4层/传输

终端设备连接，发起DHCP discover广播，DHCP收到了discover广播，DHCP利用radius协议对AAA服务器进行请求认证，AAA返回认证结果，成功则分配IP给终端，完成IP配置后，回应DHCP服务器。

#### DHCP relay/中继

大型网络中，DHCP部署于顶层，而设备可能处于不同的子网中，这是ralay agent起到一个代理作用，他部署在里该设备最近的网关或者路由器上，当设备发起DHCP广播后，首先由ralay agent进行请求接受，然后ralay agent进行发送单播给DHCP服务器请求分配IP，然后DHCP回应给ralay agent。设备完成IP分配后，网关进行DHCP中继，返回给DHCP服务器。

![[Pasted image 20241230134433.png]]

#### DHCP指纹

一种比mac认证，IP认证，账户认证更为严格的认证方式。

### ARP

arp是为了寻找对方mac地址而设计的，arp表存储着关于IP地址与mac地址的映射。在开始，发起终端发起arp请求，首先检查本机缓存，有对应条目直接发送。没有则会携带目标设备的IP地址，会走到最近的子网（交换机或路由器），发起arp广播，arp广播后如果请求终端在一个子网内，请求终端发现arp请求目标地址是中继，则响应arp请求给发起终端同时缓存arp信息。

#### arp代答

主要针对于哑终端还有跨子网通信，哑终端就是类似打印机这种设备，他不具备完整操作系统能力，所以对于arp报文响应能力有限，需要一个设备进行代理回答arp信息。比如当前有一个哑终端在管理系统内部开户，然后现在有一个终端需要对哑终端进行请求，那么首先会向网关进行问询，当请求的终端位于同一vlan和同一网段下，则网关进行代答。跨子网通信也是如此，当一个终端隐藏在一个子网内部，另一个子网无法与其通信，但是可以利用网关的arp代答机制，让网关进行代理回答。arp代答本质上也依赖于网关的arp表信息。

# mac地址

mac地址在二层环境下，源机器发出的以太网帧内部的源mac地址不会被修改，但是当经过三层机器如三层交换机或路由器，这个以太网帧的源mac地址会被修改为三层机器地址。

### SLAAC与ICMP协议

#### ICMP 3层/网络层

ICMP是三层协议，与IP协议同一级，常见的ping命令与traceroute命令都是基于ICMP协议。ICMP相较于IP协议是可靠传输，也就是和TCP差不多（TCP基于IP），常规三层IP协议在出现丢包时，无法通知上层也就是四层也就是传输层出现错误，但是ICMP可以，当我们使用ping命令时，出现目标主机不可达，ping就会表现为丢包或者请求超时。

#### ICMP代答

与arp代答类似，都是距离设备最近的路由或者交换机负责代理ICMP答复。常见于比如尝试知晓另一个子网的某个设备是否在线，利用ping命令，但是目标设备不会答复，会由网关答复。

#### SLAAC

SLAAC基于ICMP实现的一个类似于DHCP协议的协议。如果一个IPV6网络没有配置DHCP服务器的话，对路由配置SLAAC一样能实现设备的IPV6地址分配。过程如下

设备发起RS请求或静默等等路由器的RA广播

设备收到RA广播后读取RA广播携带的网关，网络前缀，DNS地址和其他配置信息等等。

设备根据网络前缀尝试生成一个IPv6地址，然后发起DAD（广播），检查该地址是否重复。

### MDNS协议

一种广播协议，

### SNMP 7层/应用层

一种网管协议，主要是对各种网络设备如交换机路由器进行管理的协议，通过在设备端口部署snmp agent，在管理侧部署snmp manager，即可监听所有带snmp agent设备的cpu状态，丢包数量，配置ip地址，vlan等等信息。

# 路由
## 静态路由

管理员手动配置路由信息和下一跳路由信息
## 直连路由
当路由器的某个端口配置了IP地址并启用后，路由器会把这个IP地址直接加入进路由表中。
### 动态路由
## 默认路由
可配置的处理未被其他路由条目所匹配的数据包，比如我现在一个数据包的无法通过路由表其他条目找到路径，则采用默认路由转发
## 路由汇总

一种合并多个连续子网未一个更大的网络地址技术。基于子网划分技术，取得两个IP网络地址下最长公共前缀，最好转换为二进制计算。

- `192.168.1.0/24` = `11000000.10101000.00000001.00000000`
  
- `192.168.2.0/24` = `11000000.10101000.00000010.00000000`
  

如此，两个IP地址前22位相同，则汇总网络地址位192.168.0.0/22
# 交换
交换机的三种转发方式。
`泛洪`:如果转发目标地址不在arp表中，也就是不知道对方地址，就泛洪
`转发`:如果能够找到目标地址，则直接进行转发。
`丢弃`:顾名思义，丢失该包
# vlan之间的数据转发

## VLAN

vlan是一种虚拟局域网，主要用于隔离广播域。他不同于真实的lan，真实的lan使用路由隔离，而vlan采用虚拟路由（SVI）进行隔离。



## SVI

SVI是交换机虚拟接口，他本身主要是用于vlan的通信，vlan和lan一样，默认隔离，所以两个不同的vlan之间无法直接进行通信，需要通过配置svi才能进行通信。



### 单臂路由

从交换机接一个trunk线路到路由的一个端口，然后配置路由器的trunk端口出多个逻辑端口对应vlan，然后可以一个网口实现对vlan数据的转发，但是这一个网口需要抗住下面所有链接设备的流量，极其容易产生瓶颈

### 多臂路由

本质上就是所有设备连接到路由器上，优点是每个线路单独处理，不会基本正常情况下没有瓶颈，缺点是布线复杂，一个设备一根线

### 三层交换

一种高级别转发设备，可实现类似路由设备功能，既有交换功能也有转发功能，按常规交换机使用，接入到该交换机，不同vlan之间可以进行数据转发。

  

## NAT 网络地址转换

#### SNAT 源网络地址转换

源NAT指的是数据包从内网发到外网经过的NAT流程，具体细节为，内网数据包在发出时，会记录原本的私有地址和端口号，然后路由器对内网地址转换为路由器的地址，然后路由器发出，响应到达后，检查NAT表找到这个链接的与内网地址端口的映射，然后NAT数据响应包的目的地址端口为内网的地址和端口。

#### DNAT 目标地址转换

目标地址转换是数据包从外网发到内网的经过的NAT流程，外网尝试访问内网数据，首先会发送数据包到防火墙，然后根据规则确定是否进行转换，如果确定转换，则转换目标地址为内网地址端口。

### NAPT

在进行地址转换的同时还进行端口转换，可以实现多个私网用户共同使用一个公网IP地址上网。NAPT根据端口来区分不同用户，真正做到了地址复用。

# VRRP

虚拟路由冗余协议，与多实例物理机虚拟化为一个机器类似。通过虚拟化多个物理路由为一个逻辑路由，可以防止单点故障

## 选举机制

每个路由设备都可以配置一个优先级，数值越高优先级越高。初始状态下，具有最高优先级的路由器将成为主路由器，负责转发流量。如果主路由器不可用，那么次高优先级的路由器将晋升为主路由器。此外，VRRP还支持抢占模式，即如果一台更高优先级的路由器重新上线，它可以抢占当前的主路由器位置。每个

在VRRP中，“虚拟化”是指创建一个由一组真实路由器组成的虚拟路由器。这个虚拟路由器拥有自己的IP地址和MAC地址，这些地址对外部网络而言是唯一的，并且是不变的。所有参与VRRP组的路由器都会监听这个虚拟路由器的状态。当主路由器正常工作时，它会定期发送VRRP通告信息给组内的其他成员，表明自己仍然在线。一旦主路由器停止发送通告，备份路由器就会根据预定义的规则选择新的主路由器.优先级相同的路由器则以接口的IP地址，谁最高，谁优先级大。

Ldap

# nops

本质上就是network operation的缩写，更为常见的名字为netops，本质就是网络运维组件，由后端组件与部分python编写的组件

  

# radius认证协议

![](https://biavdbz3ln.feishu.cn/space/api/box/stream/download/asynccode/?code=ZGRjMWE2MDdiYjk1ZTAzMWMxOGEyYjRlYjI0NWVlMzdfVVM1WG1WcER0RVZRNWhnRVBORE1Qd0M3TERMbmNRZHJfVG9rZW46TmNDN2I0Q1Ixb204NVB4MTNha2NIeFd6bjRnXzE3MzU1MzczODY6MTczNTU0MDk4Nl9WNA)


[华为知识，Radius协议](https://support.huawei.com/enterprise/zh/doc/EDOC1100086522#zh-cn_topic_0164573774_dc_fd_aaa_0005_3_mmccppss_id1)
## COA/DM

|报文名称|说明|
|---|---|
|CoA-Request|动态授权请求报文。当管理员需要更改某个在线用户的权限时（例如，管理员不希望用户访问某个网站），可以通过RADIUS服务器发送一个动态授权请求报文给RADIUS客户端，使RADIUS客户端修改在线用户的权限。|
|CoA-ACK|动态授权请求接受报文。如果RADIUS客户端成功更改了用户的权限，则RADIUS客户端回应动态授权请求接受报文给RADIUS服务器。|
|CoA-NAK|动态授权请求拒绝报文。如果RADIUS客户端未成功更改用户的权限，则RADIUS客户端回应动态授权请求拒绝报文给RADIUS服务器。|
|DM-Request|用户离线请求报文。当管理员需要让某个在线的用户下线时，可以通过RADIUS服务器发送一个用户离线请求报文给RADIUS客户端，使RADIUS客户端终结用户的连接。|
|DM-ACK|用户离线请求接受报文。如果RADIUS客户端已经切断了用户的连接，则RADIUS客户端回应用户离线请求接受报文给RADIUS服务器。|
|DM-NAK|用户离线请求拒绝报文。如果RADIUS客户端无法切断用户的连接，则RADIUS客户端回应用户离线请求拒绝报文给RADIUS服务器。|

# NAC 网络接入控制

# VXLAN 虚拟扩展局域网

一种隧道技术，该协议的主要目的是让虚拟机摆脱二三层网络限制，可以实现大范围甚至跨地域的大规模无障碍迁移。此种隧道技术，就是把源以太网报文作为载荷，再次封装进udp报文，使源以太网帧被传输层传输，这样就类似于在原本的网络链路上基于UDP又封装了一个新隧道出来。本质上而言vxlan的巨大优势是由于以太网帧的重新封装带新以太网帧上，这样可以做到一个vxlan下二层及以上信息的共享，最本质的就是会话信息，vxlan下多个设备可以无缝迁移会话信息，在没有vxlan情况下，单个设备上的会话信息是无法做到无缝迁移到另一台设备上的·

## VXLAN和VLAN之间的区别

1. 支持设备数量区别 传统的VLAN标签只支持4096个左右的设备，即使开启qinq，数量为4096*4096个，但是对于大型虚拟机集群和大规模组合系统来说，仍然数量不足，vxlan使用了一个新的新的VNI标识，有24位，可提供约为16m个租户标识能力。
   
2. 以隧道方式构建局域网
