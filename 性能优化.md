# 池化
池化本身是利用缓存提高性能的一种办法，同时也有那么一丝多路复用的味道，常见池如线程池，net.Conn连接池，buferr池，本质就是利用预加载的方式初始化一群结构，然后所有获取都从池中获取，用完后放回池中，主要是对于一些高频使用的复杂对象，每次使用时初始化和销毁都需要一定的开销成本，通过池化技术，可以直接拿去初始化完成的结构使用，同时由于使用后返回池，达到复用的效果，同时也可以控制初始化的结构数量，优化内存利用
# 多路复用
多路复用的本质是采用一个代替多个来提升任务效率。最常见的软件内部的多路复用是IO多路复用，在现代计算机中，IO是不需要CPU参与的，有专门的DMA硬件进行IO处理，CPU专注于密集计算任务。对于常规的阻塞式IO，多个线程独立发起IO系统调用后，在系统未进行返回之前，线程进入阻塞态，不消耗CPU，但是这个时候CPU是空闲态（在单任务的理想情况下）IO多路复用提出的原因就是为了更好的节省CPU资源和CPU资源利用的最大化，当多路复用后，只需一个线程管理IO状态，其他线程无须陷入阻塞态，首先节省了线程上下文切换带来的性能消耗。与IO多路复用更为常见的搭配是非阻塞式IO，也就是当IO事件未就绪，则立即返回，不进行处理。这样可以实现单线程的IO密集型应用的超高性能，单线程轮训多路复用器，多路复用器非阻塞态迅速返回，如果存在就绪IO，则轮训线程可以进行处理，处理完毕后再次轮训，同时如果配置线程绑定，可以实现单核心的持续利用，对于大量的密集的短IO（如网络IO），单线程即可驱动非常多的链接。
# 分组
分组是计算机优化手段中再常见不过的一种方法，小到分组排序算法，分治算法，大到海量数据的流处理等等，均用到了分组，分组几乎是所有算法的优化基础，包括树分支，哈希等等，本质均是采用了分组提高了性能。在高性能编程中，分组思想同样是一个非常重要的理论，比如kafka消息队列，通过分组出多个消息队列横向扩容，多队列并行，同时结合分布式，理论上处理流量能力与节点数量可以实现线性增长。
# 最短阻塞路径
最短阻塞路径是在软件开发过程中，梳理数据流在函数中的流向，为了实现快的响应速度，更低的延迟，需要对整个流程中，与核心流程无关的调用或处理均进行异步。以最常见的认证举例，如果要优化一个认证流程，使其响应更快，我们可以对其部分流程异步化，比如异步日志，比如登录通知，事件下发等等，这些流程逻辑上将确实是登录验证成功后就进行处理，但与核心认证流程完全无关，异步化完全可以提升接口性能。

# 哈希时间轮