
本文依照mit6.824分布式系统理论进行记录说明
## 分布式系统三大特性（CAS/CAP理论）
## 可扩展性/分区容错性
在常规CAP理论里面，为分区容错性。CAS里面为可扩展性。
分区容错性：
可扩展性：此为MIT6.824课程里提到的，对于部分分布式软件，我们需要机器数量与集群性能的正相关。
## 可用性
可用性代表分布式系统软件的可用性，单机系统的可用性最低，当出现程序宕机或机器关闭时，系统不可用。
## 一致性
一致性代表整个分布式系统软件中数据的一致性。

# 满足CAS/CAP理论的常见软件。
## ETCD
etcd是典型的满足CAS/CAP理论当中关于可用性与数据一致性的典型应用。对于不开启读副本的情况下，也就是对于写操作，节点数量与系统性能不相关。为了保证整个系统的数据一致性，只有master节点接受写操作，所以节点数量越多，越能保证系统的高可用性。对于可扩展性也能满足
## 常规高流量软件
在一些有着高流量的软件系统中，会保证CAP理论的可用性与分区容错性，避免节点之间数据的强线性一致性，保证系统的最终一致性。
如一个高流量分布式系统中一个用户发起更新用户名的操作。这个请求会被转发到分布式系统的一个节点，然后写入数据库，假设由着多库多表，然后数据库之间进行同步，这个过程并不强线性一致，也就是发起修改后，在客户端可能在一段时间内并不会更新修改，或者其他用户看不到该用户的修改，正因为不强线性一致，系统可以不用阻塞，可以承担更大的流量。

## 拜占庭问题
拜占庭问题是对分布式共识算法的一种描述形式，不过这个问题相比共识算法更加复杂，因为他引入了对于不可信任节点与不可靠通信问题。拜占庭问题相当于保证一个分布式软件系统中在存在恶意节点与不可靠通信时，仍然保证分布式系统的正常运行。

从三个拜占庭将军之间就可以得到一个问题：
在未出现叛徒时，即每个人将军发出的命令都是相同的。如果三个将军中由两个认同可以发起进攻，那么系统会检查到2:1，发起进攻成功。
在出现叛徒后，有一个人将军发起的命令对于另外两个而言，是不一致的，那么就会完全干扰整个系统的投票。
对于一个拜占庭的系统，假设由n个节点，其中最多有着f个节点是恶意的，那么满足拜占庭系统的条件是$n>3f$

## 分布式事务
分布式事务类似于数据库事务，但是分布式事务横跨多个节点，也是对于分布式系统中的一致性的保证，在分布式系统中过分强调一致性时，会极端影响性能，要结合业务需求进行选择，如强线性一致性与分布式事务。
`两阶段提交`：分发起事务阶段与事务提交阶段。发起事务阶段要求协作者进行事务操作资源的锁定，锁定完成后执行事务的执行单元并做回复，进行下一个阶段，事务执行阶段，向所有参与者发布commit请求，并收集回复commit。
`三阶段提交`:与两阶段提交基本类似，新增加一个预准备阶段，检查事务所需资源是否可用，防止两阶段提交的准备阶段发生阻塞。

## 同步状态机与数据同步

同步状态机是种在分布式系统种用于数据同步的非常常见的一种机制。同步状态机的操作对象并不是数据，而是日志，也可以称为操作。同步状态机本身的原理就是，所有的状态机经过一系列同样的同步操作，会保证所有状态机本身的状态一致。

常见的数据同步与常见缺点如下：
- 全量同步：通过拷贝一个数据容器的整个数据到另一个数据容器，缺点就是在海量数据下，想完整拷贝一整个数据源需要非常大的网络带宽，且十分消耗

### 分布式系统
分布式系统主要涉及到redis缓存与mysql之间的数据同步，
单机系统
# binlog
mysql一种记录数据库信息的一种方式，从名称可以看到是一种二进制log信息，非常紧凑，binlog记录了数据库所有的操作历史，所以我们可以依赖于binlog重放可以实现数据恢复功能，同时由于记录了操作，我们可以利用log+消息队列实现写入事件的推送，这种对于消费者未redis或其他数据库之间的主从复制提供了便利