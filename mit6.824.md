
## 分布式理论基础
CAS理论：
## `Scalability`:可伸缩性，可扩展性。
在瓶颈在处理能力上时，通过增加集群的处理机数量带来集群的处理能力的提升。当瓶颈在如数据库上时，增加集群处理机基本不会带来任何提升。
## `Availability`: 可用性。
故障在分布式的环境中非常常见，也许我们见过不少单台设备运行很长时间而不中断服务。但是几乎不存在一个运行很长时间，没有一个设备出现故障的分布式环境。可能由于分布式环境中的处理机设备质量的参差不齐。可能意外断电，设备过热停止服务，网络线缆损坏等等。分布式系统的可用性目标就是弱化故障带来的影响，比如常见的通过同步复制到两台机器上，即使一台故障另一台仍然能够进行服务，更强大的甚至可以配置不同区域的多活保障，即使某地遇到地震海啸等机房损毁事件的发生，其他地域仍然能够进行服务保障且数据不丢失。可用性的保障主要来源于两种：高可靠的存储设备或复制。
## `Consistency`:一致性。
当一个分布式环境中，存在一个数据的多个副本时，对于数据的操作就会涉及到一个数据一致性的问题。假设两个不同的用户操作了两个不同的副本，且互相不可知的情况下，就会出现数据不一致的情况，比如银行系统你在某地存钱100元，但去另一个地域后，发现你存的钱没有了，这是不可忍受的。所以在多副本系统中，保障数据的一致性非常重要。一致性可以分为线性一致性（强一致性）和弱一致性。每种一致性都有自己的可适用范围，强一致性能够保障你的所有操作行为都是可预测的，不存在多副本操作时出现不一样的情况。但是强一致性的实现成本是高昂的，他需要通过副本通信来实现数据的一致性，同时注定了强一致性系统的性能不如弱一致性。

## `MapReduce`: 并行处理的军刀
MapReduce是由Google提出的一种大规模数据的处理方案。Map和Ruduce分别是两种处理函数，Google设计这种处理方案的背景是，需要一个简单的方案去对当时Google的搜索引擎产生的大规模的索引，日志数据进行分析。为了尽可能的提高效率和简化使用流程，提出了MapReduce思想。首先MapReduce的输入源可能是来自于GFS的分割后的Chunk，或者一个网页文件。工程师通过编写Map和Reduce函数，提交的文件会由Map进行预处理，Reduce进行二次处理，Map产生的一个在论文中称为中间数据的形式。而Reduce本身就是对Map产生的结果进行二次处理，当然，你可以编写多个MapReduce函数，把一个MapReduce函数的产出作为另一个MapReduce的输入。这一点其实非常像`责任链`模式的云端推广，Map和Reduce包括Shuffle过程也都像是一个责任链的责任Handler。

mit6.824中提到了一个非常经典的MapReduce案例。通过输入多个文本文件，通过MapReduce来进行文本中每个字出现频率，这个里面Map就是负责拆分所有的字，而Shuffle负责统计，Reduce负责整合。

# GFS
`GFS`：Google File System（谷歌分布式文件系统）
## 大规模分布式存储系统的难点
1. 最初的出发点：性能加成
   利用多个设备进行存储文件，实现数据的的`分片`后存储，同时利用并行读取性能带来性能提升
2. 大规模分布式系统的痛点：故障
   前文提到，当集群内计算机数量越多时，发生设备故障的概率就越高，在发生设备故障后，如何保障系统的正确运行。`容错`
3. 提高容错率的实现：复制
   通过多副本的复制，保障单点故障带来的服务不可用和数据丢失。
4. 数据复制的终极挑战：一致性问题
   当通过多副本的形式实现服务的高容错，不可避免的会涉及到数据副本不一致的问题
