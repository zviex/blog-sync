# 配置中心
配置中心的功能无外乎就是提供服务发现，服务注册，配置管理与推送。

## 服务端
配置中心的服务端提供以下业务，服务注册，针对每个服务（客户端）的配置，提供注册接口，将其封装为特定的结构，然后存储在配置中心的服务端中，同时当客户端的配置发送变更后，配置中心会立即通知每个连接的客户端最新的配置内容。

## 客户端
配置中心通常会提供客户端的服务，客户端的功能主要是实现服务发现。服务发现的最本质的原理就是，当应用程序启动后，会立刻读取服务启动的元数据，比如最简单的IP，端口号。将其推送到配置中心的服务端，其实主要就是这个功能。


