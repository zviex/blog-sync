# HTTP 1

HTTP 1.0中，交互模型为，一次请求，发起一个tcp链接，即用即销毁，tcp的链接建立成本本身是很大的，所以这个算是http1.0的最大的一个弊端

HTTP1.1 着重解决了http1.0中，tcp链接不能复用的问题，HTTP1.1引入了tcp链接复用，让多个请求共享一个tcp链接，降低tcp链接开销成本。但是引入了http的队头阻塞的问题。



`HTTP1.1队头阻塞问题`：http1.1中，每个请求是独立的，但是tcp链接复用了，所以当一个大请求正在进行该tcp上进行耗时消息发送时，所有的其他请求都会进行等待，导致通信延迟增加。

# HTTP2

http2.0 着重解决了http1.1中关于队头阻塞的问题，他采用了类似于WebSocket的frame方案，把一个消息进行分小片并进行编号，这样的话，当一个大型的请求正在同一个tcp链接上进行写入时，会被分片写入，由于写入单位时片，其他的请求和这个大请求本身可以进行乱序写入传输，这样即使大请求本身并没有发送完毕，不影响一些小请求的通信，他可以正常的进行通信，大大减少了http1.1队头阻塞的问题。

# HTTP3

Http3 http3.0协议直接抛弃了tcp协议，因为tcp协议本身天然存在一个特性，就是丢包重传，这也是为什么tcp是能够保障消息的安全送达，不会导致如udp类似的丢包，当一个数据包没有收到，就会强制要求客户端重传，这样保障了消息的完整性。但是这种操作本质上也增加了延迟，因为当丢失数据包等待客户端重新传输的同时，其他以及就绪的数据包必须强制等待该数据包的到达才会交付给应用层。http3直接采用了udp上的quic协议，quic是用户态的协议，他与tcp类似，他也可以保障数据的完整性，也会重传。但是他抛弃了再内核态整体就绪后交给应用层的这一步，他把每个数据都会直接上报给应用层。

http3的场景中最明显的一个就是，比如现在有100个数据包，1-50和51-100分别属于两个文件，当第49个数据包丢包后，再tcp场景下会要求客户端进行重新传输49号数据包，这个时候51-100号数据包由于49号未能到达被强制不能提交到用户态程序。如果时udp，quic本身也会要求客户端重新传输第49个数据包，虽然这个时候第一个文件由于数据包不完整，不能读取一个完整的文件，但是这个时候不影响第二个文件的读取和完整交付，这个就是http3.0解决的问题

# CSRF攻击与同源策略
浏览器的同源策略是三元组
1. Protocol
2. Addr
3. Port
当三者都满足时，则认为是同源。不同源访问会发现浏览器控制台报错，无法加载数据。
但是这个同源策略是浏览器的一个功能，他主要是浏览器拒绝读取非同源的服务器的响应，本身服务器是可以进行响应的。不然的话，我们平常的api测试工具，还有curl之类的工具请求会被拒绝。

因为拒绝行为由浏览器处理，所以引发一种攻击方式，CSRF 跨域伪造请求攻击。攻击者通过各种诱导到攻击者的页面，里面嵌入一个目标网站的链接，当我们执行了这个恶意js后，浏览器会拿取本地的目标地址的cookie附带在这个请求上执行操作，虽然同源策略会拒绝，但是对于一些没有两步验证的接口，这样可以直接实现浏览器进行攻击。


解决方案：CSRF Token


# TCP/UDP

## TCP
三次握手
四次挥手

滑动窗口限速

### 粘包

首先tcp本身是面向数据流的协议，也就是说tcp传输的是纯字节流，所以对于tcp协议本身是不知道上面传输的数据那些是在逻辑上属于同一个包的。所以这个就是粘包，对于tcp的协议，粘包的解决方案就是添加终止符号。

我们以http为例，http协议中，主要进行截止分离的主要是body部分，Header 使用 `\r\n` 作为每一行的分隔符，并使用连续的 `\r\n\r\n`（空行）作为 Header 结束的标志。对于body主要有以下几种的处理方式。

1. Connection Close
   这个主要是在http1.0中，因为那个时候的http是，请求一次发起一次tcp链接，不存在tcp链接复用，一个链接关闭了，证明这个请求结束了。也是body的截止标识符

2. Transfer-Encoding: chunked

   请求将会分片，分片的逻辑如下:
   ```text
   HTTP/1.1 200 OK
   Content-Type: text/plain
   Transfer-Encoding: chunked
   
   hex\r\n // hex为十六进制的长度数字，代表这个块内数据长度
   {hex-length}\r\n
   0\r\n // 这个代表请求结束
   \r\n // 表示没有 Trailer 部分或 Trailer 部分结束
   
   
   ```

3. Content-Length
   这个是除了chunked以外的最常见的一种，这个请求头直接标识了body的长度，当header部分读取完成后，直接按照length长度读取指定字节数，即可获取Body的边界

### 队头阻塞

还是tcp的面向数据流的特性，所以当tcp上的数据的数据包丢失了一个，tcp就会触发重新传输的机制。所以当前的一个数据流上，可能包含了逻辑上的`MsgA`和`MsgB`两个数据的内容。但是当`MsgA`的最后一个数据包丢失后，`MsgB`并不能直接提交给应用层，还是那句话，tcp不知道逻辑消息的边界，只能等待`MsgA`的数据包重新传输后，保障数据一致，然后提交给应用层。这个就是tcp的队头阻塞，所以从应用层处理层面来说，`MsgB`就绪了，就应该让MsgB先给我处理，而不是傻乎乎的等待`MsgA`的丢失数据包重传。但是这个是tcp的可靠性传输实现机制导致的，这个无法避免（这也是为什么http3上http协议彻底抛弃了tcp作为传输协议）

## UDP

### 协议特性

UDP不同于TCP，UDP为面向数据报的协议，所以每一个数据包就是一个完整的数据，所以UDP是无状态的，你可以说发出去的每个数据包互相并无关联。且UDP只管发出去，送不送的到UDP不管。你只能认为发向同一个远程地址的同一个端口的数据包之间是有状态的。但是这只是猜测，具体是否属于同一个状态需要看UDP内封装的识别特性。（所以这也是在UDP开发时，我们会发现，对于处理了任何的一个UDP报文后，我们似乎并不能针对这个UDP报文的Conn发送响应？这个就是UDP的无状态特性）

### quic/kcp

既然UDP本身面向数据报且独立，同时每个UDP报文是存在长度限制的，你并不能说我把一个电影通过一个UDP报文发送出去。那么我们是否可以尝试把一个大文件切割为独立的数据包后通过UDP发送出去呢？但是这样会面临一个问题，一部电影发送出去后，我并不能确定对方完整的收到了整个电影，而且面临不稳定的网络环境，就会发生丢包，且UDP没有重传的机制，如何保障安全？

UDP协议上存在两种协议，一种是`QUIC`协议，一种是`KCP`。他们两种协议都有一种特性，在UDP上实现了可靠传输。他们的可靠传输的思路如下：

1. udp面向数据报且有单个大小包限制？利用算法切割一个大文件为多个UDP数据包发送出去。
2. UDP不可靠？不知道那些包被丢弃了？在每个UDP包中塞入一定的头部，比如序号，远程服务端根据序号即可确定丢失了第几号的包。
3. 发生了丢包？服务端收到了序号为1,3的包，证明该链接上存在丢包，且丢失了序号为2的包。触发QUIC/KCP的重传机制，重新传输2号包。



> 为什么HTTP3选择了QUIC作为应用层传输协议？
> 答：UDP由于天然的面向数据报的特性，所以每个数据包直接关联性并不大。所以出现了某个逻辑消息的消息包丢包了，重新传输这个消息包即可。并不影响其他的逻辑消息交付给用户态。