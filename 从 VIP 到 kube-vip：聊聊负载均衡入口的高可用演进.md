
## 一、问题的起点：套娃式的单点困境
一个典型的 Web 架构演进是这样的：
```
单机服务 → 加 Nginx 反向代理 → 多台后端
```
多台后端解决了**服务**的高可用，但问题被上移了一层：**Nginx 自己挂了怎么办？**
直觉的解法是继续往上加——两台 Nginx，前面再放一台代理来分发。但最顶层那台永远还是单点，这就是“套娃困境”：**每一层的高可用方案，都会制造出新的单点**。
跳出套娃的关键在于换一个思路：**不再依赖更高层的组件来选择转发给谁，而是让 IP 地址本身变成一种可以“浮动”的资源**——这就是 VIP（Virtual IP / 浮动 IP）。
---
## 二、VRRP：让 IP 成为可竞争的资源
VRRP（虚拟路由冗余协议）的核心设计：
1. **选举**：多个节点组播 VRRP 通告（224.0.0.18，协议号 112），按 Priority（1-254）选出 Master；优先级相同时比较接口 IP 大小。
2. **心跳维持**：Master 周期性发通告（默认 `advert_int = 1s`），Backup 只做一件事——收通告、续命计时器。连续 3 个周期收不到，就认定 Master 死亡，触发重新选举。
3. **VIP 接管**：新 Master 把 VIP 绑到自己网卡上，流量无缝接力。
一个经典坑：VRRP **只检测节点存活，不检测服务健康**。机器活着但 Nginx 挂了，VRRP 是不会切换的——这正是 Keepalived 存在的意义（后面讲）。
### 免费 ARP：切换生效的“临门一脚”
IP 属于三层，但最终通信走的是二层的 MAC 地址。VIP 漂移后，IP 没变，**变的是 IP 背后的 MAC 映射**。
新 Master 当选后必须发送**免费 ARP（Gratuitous ARP）**——一个“源 IP = 目标 IP = VIP”的特殊广播报文。它的唯一使命是**强制刷新全网设备的 ARP 缓存表**，让交换机和客户端把 “VIP → 旧 Master MAC” 改写为 “VIP → 新 Master MAC”。
这条没发好，就会出现经典故障现象：*VIP 漂移成功了，但业务不通几分钟*。所以 Keepalived 默认会冗余发送多个 GARP 报文来对冲网络设备的缓存老化时间。
### VRRP 的天然局限
- **要求同二层**：所有节点必须在同一个广播域内，无法跨三层、跨地域。
- **脑裂**：心跳网络分区时可能出现双 Master，同一 IP 在二层出现两个 MAC，流量随机命中。
- **主备模式，备机闲着**：流量全走 Master，单 VIP 有吞吐瓶颈。改进方案是双 VIP 互为主备，或直接用 ECMP + BGP 多活。
- **无横向扩展语义**：只有主备，要扩展得引入 LVS/DPVS + ECMP。
---
## 三、Keepalived：VRRP 的工程化落地
Keepalived 是 VIP 高可用事实上的标配，它的架构只有三个模块，分工清晰：
| 模块 | 职责 |
|---|---|
| **Core** | 解析配置、主调度循环，拉起另外两个模块 |
| **VRRP Stack** | 协议状态机、选举、收发通告、VIP 绑定、GARP 发送 |
| **Checkers** | 健康检查，把“服务死活”接入切换决策 |
三个模块构成一个闭环：
- **VRRP 状态机**是骨架：INIT → BACKUP → MASTER，定义了角色和切换规则；
- **健康检查**是神经：通过 `vrrp_script` 周期探测本机服务（TCP/HTTP/自定义脚本），失败时按 `weight` 降低优先级。一旦降级后的优先级低于对端，节点**主动让出**，触发切换。这把切换决策从“机器存活”升级为“服务可用”；
- **VIP + GARP**是肌肉：把决策真正落成网络流量的重定向。
恢复场景还有个细节：默认**抢占模式**下，原 Master 恢复后会抢回 VIP，代价是恢复瞬间多一次抖动；生产环境更常见的是配 `nopreempt`，让现任一直干到故障为止——更稳，但主备身份会漂移不定。
**优点**：轻量（约 2MB）、成熟稳定、配置简单、与 LVS 深度集成，主备场景开箱即用。
**局限**：严格同二层限制、脑裂无法根除、功能单一（只管 VIP 和健康检查）、对微服务和云原生场景水土不服。
---
## 四、kube-vip：云原生时代的一体化方案
到了 Kubernetes 时代，问题变成了两个：**控制平面（API Server）的高可用** + **Service LoadBalancer 的 IP 从哪来**（裸金属环境没有云厂商 LB）。kube-vip 用一个组件同时解决这两件事。
它的设计哲学是：**用 Kubernetes 的方式解决 Kubernetes 的问题**——以 DaemonSet/Static Pod 部署，用 Lease API 做 Leader Election（而不是 VRRP），靠监听 API Server 感知状态。
### 与传统方案的本质差异
| | Keepalived | kube-vip |
|---|---|---|
| 选举机制 | VRRP 协议（独立于 K8s） | K8s Lease（依赖 API Server） |
| 部署形态 | 集群外的独立进程 | 集群内的 DaemonSet |
| 覆盖能力 | 仅 VIP 漂移 | 控制平面 HA + Service LB |
| 网络通告 | 二层 ARP | ARP / **BGP** / WireGuard 可选 |
| 控制平面 HA | 需额外配置 | 原生支持 |
其中 BGP 模式值得一提：每个节点与网络中的 BGP 路由器建会话，将 VIP 作为路由前缀宣告出去，流量靠路由协议分摊到多个节点——这是**真·多活负载均衡**，摆脱了二层限制和主备模式的吞吐瓶颈，代价是需要支持 BGP 的网络环境。
### 选型建议
- **给裸金属 K8s 新建集群**：首选 kube-vip，一体化、声明式、运维成本低；
- **传统主备 VIP、追求极致切换速度**：Keepalived 依然是瑞士军刀；
- **需要管理整个服务集群（数据库、文件系统等复杂资源）**：Corosync + Pacemaker；
- **公有云**：直接用云 LB，别自己造轮子；
- **跨地域大规模分发**：Anycast（多实例同时宣告同一 IP，靠 BGP 最短路径就近引流，无主备、无脑裂，但要求应用无状态）。
---
## 五、写在最后
回头看这条演进路线，其实是一个不断“消除上层单点”的过程：
```
单点 Nginx
  → VIP + VRRP：把选择权交给网络协议本身
    → Keepalived：补上服务健康感知
      → kube-vip / Anycast / ECMP：走向多活与云原生
```
Keepalived 像一把精巧的瑞士军刀，专注做好主备切换这一件事；kube-vip 则代表了社区用 Kubernetes 原生方式简化自身复杂性的努力。它未必更快，但**简洁性和生态集成的价值，往往超过那几秒的切换时间差**。
理解每层方案解决了什么、又留下了什么问题，才能在合适的位置做出合适的选型。
---
*如果有读者在实际操作中遇到 VIP 漂移后业务短暂不通、双 Master 脑裂等问题，欢迎留言讨论——那些坑大多都能在 GARP 和仲裁机制上找到答案。*
