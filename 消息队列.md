Kafka
NSQ
NATS(JetStream)

## nats的发布订阅模式与队列组的区别
发布订阅模式，指的是一个topic会一直生产者往里面写，所以订阅就是，各个消费者独立的去订阅这个队列里面的信息。
这两个模式在逻辑上来讲，主要区分度是在是否重试上。一般情况下需要发布订阅的，不一定严格需要重试。但是生产者消费者这个模型下的，基本上都是要带重试的。

但是在具体实现上，基本都是通过一种方案实现。

commitlogs+日志位点。一个topic会被严格顺序执行顺序append写入到一个或多个文件。在文件内严格有序，同时消费者不管是属于订阅还是消费，都会维护自己的消费指针，如果有重试，那么消费指针会记录当前的日志并重新投递。这种模式下，同一个topic下的所有消费者消费信息时，不会发生显式的消息分发拷贝，只会使用指针，他们完全共享该topic，同时每个消费者的进度互不相干，一个消费者落后，不会导致其他消费者降速。
