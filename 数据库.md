mysql 最常见的索引类型
1. B树索引：
   最常见的索引，支持了范围查询，排序等效率效率高，是默认索引

2. 哈希索引：
   类似于哈希表，性能极高，当进行where索引字段的时候，就是o1的时间复杂度就可以找到数据，但是范围查询，排序等效率不是很高。

索引种类
1. 常规索引
   就是常规的btree，主要用来加速查询
2. 唯一索引
   在常规上加入了唯一限制，防止重复，但是可以有null
3. 主键索引
   在唯一索引上添加
4. 组合索引，就是对多个



回表

# 高性能游标滚动式列表展示
条件范围+键集游标是一种条件约束后采用一些可以orderby的字段的，类似游标的滚动形式展示。此种避免了传统分页带来的深分页的问题，当深分页发生后，会出现性能问题



# 可能全表扫描的场景
## 深分页
传统的offset分页方式，mysql本身并不是直接跳转到某个分页区间拉取数据，而是根据扫描到当前页的部分后，然后丢弃前面的页进行展示。所以深分页当分页到最后一页时，等同于全表扫描。

## 索引不命中
索引不命中的场景很多，包括不遵从最左匹配原则使用Like搜索，无法利用任何索引，只会走全表查询。多个单列索引导致数据库回表全表查询。